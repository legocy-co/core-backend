# docker-compose.yaml
version: '3.7'

services:

  db:
    image: postgres:14
    ports:
      - "5432:5432"
    volumes:
      - ./data:/var/lib/postgresql/data
    environment:
      POSTGRES_USER: "postgres"
      POSTGRES_PASSWORD: "postgres"
      POSTGRES_DB: "legocy_go"

